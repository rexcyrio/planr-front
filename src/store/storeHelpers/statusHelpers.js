////////////////////////////////////////////////////////////////////////////////
// Case reducers
////////////////////////////////////////////////////////////////////////////////

export const outOfSync = (state) => {
  state.status = "OUT_OF_SYNC";
};

export const inSync = (state) => {
  state.status = "IN_SYNC";
};
